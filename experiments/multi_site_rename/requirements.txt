# Multi-site / multi-identifier rename benchmark
# Colab usually already has torch + transformers; pinned loosely for portability.
torch>=2.1
transformers>=4.44
accelerate>=0.30
javalang>=0.13.0
tqdm>=4.64
requests>=2.28
